```
curl -d "word=bmw" -X POST "localhost:8080/api/v1/score"

```
```
curl -L -X POST "http://completion.amazon.com/search/complete?search-alias=aps&client=amazon-search-ui&mkt=1&q=samsung"
```


fixed
```
curl -X GET "localhost:8080/api/v1/estimate?keyWord=iphone+11"
```